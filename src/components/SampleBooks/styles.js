import { StyleSheet } from 'aphrodite';

import { indigo } from '../../constants/colors';

export default StyleSheet.create({
  sampleBooks: {
    display: 'flex',
  },

  author: {
    color: indigo
  }
});
