import { StyleSheet } from 'aphrodite';

export default StyleSheet.create({
  bookContainer: {
    position: 'relative',
    height: 'calc(100% - 20px)',
    margin: '10px',
  },
  book: {
    height: '100%',
  }
});
